# AGENTS.md

在本知识库工作时：
1. 先完整读取 `AI协作协议.md`；
2. 再读取 `加载清单.md`，只加载当前任务需要的内容；
3. 从 `10_当前工作台/` 确认目标、范围、状态和下一步；
4. 结论优先查 `30_知识主题/`，重要判断回到 `20_原始资料/`；
5. 不覆盖原件，不把 Candidate、草稿或旧结论写成当前事实；
6. 实质工作结束后更新 `维护日志.md`，并在 `99_维护记录/` 留下交付记录。