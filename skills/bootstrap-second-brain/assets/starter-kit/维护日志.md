# 维护日志

> 追加式（append-only）：只在文末追加新条目，不改写历史条目。每条按六段式写全：**来源**（本条依据什么）/ **变更**（改了什么）/ **判断**（为什么这样改）/ **验证**（用什么核对过）/ **边界**（没覆盖什么、"登记了去向"≠逐字复核，抽样须声明范围）/ **关联**（回链相关页面）。示例条目可只填有信息的段，但不得虚构。

## YYYY-MM-DD 初始化第二大脑
- 变更：建立九区目录、六个控制文件和基础模板；
- 首个真实问题：
- 验证：目录检查 / Git 初始提交 / 隔离恢复演练；
- 回滚：从 Git 初始提交恢复；
- 下一次复核：

## 2026-08-07 TwinMind Starter 宿主中立化与发布投影
- 变更：为三个 canonical 控制文件建立唯一成对 managed-block 标记；把 Python、Git、WorkBuddy、Obsidian 和社区插件明确分为 runtime-required、product-required、recommended 与 optional；Git 成为 create 硬门禁；WorkBuddy 更新为当前官方初始入口；新增 canonical-to-assets 单向同步、Starter manifest 和 `personal-vault-v1` inventory policy。
- 口径：`我的第二大脑/` 是 canonical Demo；`skills/bootstrap-second-brain/assets/starter-kit/` 是可重建发布投影，不反向覆盖 canonical。
- 验证：`test_assets.py` 12/12 通过；`sync_starter_assets.py --write` 与 `--check` 通过；最终文件数与摘要以 `manifests/starter-v1.json` 为准。
- 回滚：恢复本次 canonical diff 后重新运行同步器；不得直接把 assets 内容反向复制到 canonical。
- 下一步：U2-U9 的本地确定性实现与验证见 `99_维护记录/TwinMind第二大脑初始化Skill实施交付记录.md`；真实用户 pilot、正式发布验签和许可证仍按独立门禁推进。

## 2026-08-07 TwinMind 第二大脑初始化 Skill 本地实施收口
- 变更：实现 tool-prepare、create、verify、adopt-existing inventory、create-run resume、pre-baseline recover、外部状态 cleanup、Git baseline、Starter 投影、确定性打包与 OpenSSH 双签名验证入口；修复 plan/authorization 绑定、源与目标漂移、symlink/路径逃逸、Git hooks/filters/attributes、状态文件权限、inventory 预算和发布输入 TOCTOU 边界。
- 工具顺序：Python 3.11+ 是 runtime-required，Git 是 create 的 product-required 硬门禁；WorkBuddy 与 Obsidian 是 recommended，可明确暂缓；社区插件 optional 且默认空集合。
- 状态与隐私：平台默认状态根和显式 `--state-dir` 均使用目标外私有目录；缺少可验证卷分类 adapter 时一律为 `unknown`，confidential `runtime_state` 返回 `action_required`，不明文降级。
- 验证：完整单测、deterministic eval、Starter 同步检查、确定性 package check、Skill quick validate 与 `git diff --check` 按交付记录执行；精确文件数与摘要由当前 manifest、package 输出和最终交付回执给出。
- 声明边界：本地结构合同和临时真实 OpenSSH 双签名 fixture 可验证；正式分发仍需 Owner 外部可信材料，behavior 仍是 deterministic contract/eval，真实用户七天闭环与 field outcome 尚未完成。
- 回滚：baseline 前只删除本 run 创建且哈希未变化的非 append-only 文件；missing-target partial staging 可独立恢复；baseline 后只允许补偿提交计划或人工 Git 恢复，不宣称擦除历史。

## 2026-08-11 学习闭环与决策学习方法同步
- 变更：从实际第二大脑运行经验中提炼七阶学习闭环、三环进化模型和需求侧激活原则；新增一份可选的高价值知识学习与回放模板，并更新入口索引与运行指南。
- 口径：七阶是失败诊断器，不是新的顶层架构或必填状态；第二大脑进化以真实任务、结果偏差和模型修订为证据，不以文件、字段或自动化数量为证据。
- 隐私：仅同步可公开复用的方法，不复制个人台账、工作日报、公司资料、私有路径、内部指标或第三方 Skill 源码。
- 验证：canonical 内容完成后，由 `sync_starter_assets.py` 单向刷新 Starter 投影，并执行完整 Skill 检查；精确文件数和摘要以当前 manifest 为准。
- 回滚：恢复本次 canonical diff 后重新运行同步器；不得从生成投影反向覆盖 canonical。

## 2026-08-11 任务运行控制层
- 变更：新增 `60_任务系统/`，建立任务注册表、任务契约模板、“任务规划→总结→思考→进化”每日闭环，以及每周知识蒸馏、每月健康检查和每季度反熵审查。
- 边界：项目待办仍属于 `10_当前工作台/`，能力属于 `50_Skills/`，运行契约属于 `60_任务系统/`，运行证据属于 `99_维护记录/`。
- 来源：参考实际知识库 `60_tasks` 的“任务登记层与真实调度事实 owner 分离”方法；未复制个人收件人、内部群、绝对路径、外部账号 ID 或具体业务任务。
- 状态：七个示例任务均为 `draft`，`scheduler_owner=unassigned`；本次只建立契约，没有安装调度器、启用定时运行或扩大 AI 权限。

## 2026-08-11 第二大脑建设方法论总纲
- 变更：新增 `30_知识主题/第二大脑建设与运行方法论.md`，统一第一性原理、三层建设视角、一个事实核心与五种协同能力、九区职责、价值闭环、阶段验收和停止条件。
- 裁决：三层设计回答“如何建设”，一核五种能力回答“如何运行”，两者不映射为平行目录；贝叶斯主动学习只保留为候选过载后的排序增强，不进入第一版默认运行时。
- 来源与隐私：提炼真实知识库中可公开复用的方法，不复制个人目标、内部台账、业务数据、账号、绝对路径或运行配置。
- 状态：方法论保持 `pilot`；当前结构与本地契约检查不能代替多用户验证和长期价值结果。

## 2026-09-04 运转层迭代收口（Starter 0.2.0）
- 来源：迭代计划 docs/plans/2026-09-04-001-feat-second-brain-operations-iteration-plan.md（U11 集成单元）。
- 变更：协议三节、治理字段、任务审计字段与迁移门禁、WorkBuddy 可选映射、周报/体检/六段式日志、来源包与摄取两档、跟踪立项模板、入口登记；Starter 版本升至 0.2.0，投影与 manifest 同步刷新。
- 判断：范围按需求文档 v2 的 owner 裁决执行；示例全部虚构，脱敏红线约束文档自身。
- 验证：sync --check / unittest 113 项 / evals --suite all（safety 含三新 case）/ package --check 四命令全绿；review_due.py 对本 Demo 只读演练（48 页扫描、0 到期、4 占位跳过）；四入口命中抽查通过。
- 边界：forward eval 待发布后排期；run_evals 全绿仅证明静态合同；老用户升级为对照吸收，无自动迁移。
- 关联：CHANGELOG v1.18.0；发布前脱敏清单执行记录见该次提交描述（扫描范围为本次全部变更文件，零命中，逐项执行）。
